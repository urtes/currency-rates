spring:
  datasource:
    url: jdbc:h2:./currencies
    username: user
    password: user

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: create-drop

period:
  days: 5
  cron:
    daily: "0 0 0 1/1 * *"

    # every ten seconds, working
#    daily: "*/10 * * * * *"